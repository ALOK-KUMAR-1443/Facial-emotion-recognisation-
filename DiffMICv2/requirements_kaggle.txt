# Kaggle-compatible requirements (works with pre-installed packages)
# Install with: pip install -r requirements_kaggle.txt --no-deps

# Critical updates only (avoid breaking Kaggle environment)
peft>=0.17.0
diffusers>=0.20.0
accelerate>=0.22.0
einops>=0.6.0
ema-pytorch>=0.2.3
easydict>=1.10

# Keep Kaggle's pre-installed versions for stability
# numpy (use Kaggle default 1.26.4)
# torch (use Kaggle default)
# pytorch-lightning (use Kaggle default)
# torchmetrics (use Kaggle default)
# scipy (use Kaggle default)
# matplotlib (use Kaggle default)
# scikit-learn (use Kaggle default)
# opencv-python (use Kaggle default)
# Pillow (use Kaggle default)
# pandas (use Kaggle default)
